{
  "name": "olala7846-agent-plugins",
  "private": true,
  "version": "0.2.0",
  "description": "Development tooling for olala7846-agent-plugins.",
  "license": "ISC",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "validate": "npm run validate:manifest && npm run validate:layout && npm run validate:skills",
    "validate:manifest": "node scripts/validate-manifest.mjs",
    "validate:layout": "node --test test/plugin-layout.test.mjs",
    "validate:skills": "skills-ref validate skills/spacex-simplify && skills-ref validate skills/quiz-me && skills-ref validate skills/repo-init"
  },
  "devDependencies": {
    "ajv": "8.20.0",
    "skills-ref": "0.1.5"
  }
}
